<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hjk.transportanother" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:fullBackupContent="true"
        android:icon="@drawable/big11"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:ignore="AllowBackup">
        <activity
            android:name="ua.anon.unfeeling.transportanother.MainActivity"
            android:noHistory="true"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="ua.anon.unfeeling.transportanother.Settings" android:noHistory="true"/>
        <activity android:name="ua.anon.unfeeling.transportanother.Info" android:noHistory="true"/>
        <activity android:name="ua.anon.unfeeling.transportanother.StartContact" android:noHistory="true"/>
        <activity android:name="ua.anon.unfeeling.transportanother.Rating" android:noHistory="true"/>

        <service
            android:name="ua.anon.unfeeling.transportanother.TransportAnother">
        </service>
    </application>

</manifest>
